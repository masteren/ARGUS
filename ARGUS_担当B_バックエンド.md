# ARGUS プロジェクト — 担当B：バックエンド＋ダッシュボード（IoT）

### 📌 このドキュメントについて
これは、ARGUSプロジェクトで **あなたが担当する部分** をまとめた資料です。
- 中身: プロジェクトの紹介 → チームの分担 → あなたの担当範囲 → 開発の進め方 → スケジュール。
- **一番下に、あなた専用の「AIプロンプト」と、その使い方（AIに『プロジェクト』として登録すると毎回貼らずに済む）を用意しています。**

まず全体に目を通して、最後のプロンプトを自分のAIに設定してください。

### 1. プロジェクト紹介
**ARGUS（アーガス）** は、6脚ロボット（Freenove Big Hexapod + Raspberry Pi 5）をベースにした **AI監視ロボット** です。カメラで周囲を見て人や物を検出し、その情報を遠隔のダッシュボードで監視でき、さらに音声でロボットと対話できます。3人チームで作る、チームの主力プロジェクトです。

3層構成:
- **歩行層**: Freenove公式コード（既製・改変しない・全員で組立）
- **AI層（自作）**: 画像認識 ＋ 音声対話
- **IoT層（自作）**: Flaskバックエンド＋Webダッシュボード

### 2. チームの分担（3人）
- **担当A：画像認識** — Pi 5上でYOLO/OpenCV。人物/物体を検出してバックエンドへ送る。
- **担当B：バックエンド＋ダッシュボード（＝あなた）** — Flask+SQLite。検出を受信・保存し、Webダッシュボードで遠隔監視。
- **担当C：音声対話＋統合（任／リーダー）** — 音声でロボットと対話、歩行層との連携、全体統合。機体はCが管理。

各モジュールは疎結合です。データは **あなたのバックエンドを中心としたスター型** で流れます:
- A（画像）→ **あなた（B）へ** 検出を送る（POST）
- C（音声）→ **あなた（B）から** 最新検出を読む ＋ 歩行層へコマンド
- A と C は直接通信しない。全データがBを経由する。

つまり、AとCの両方が **あなたのAPIに依存** します。だからインターフェースを早く固めるほど、チーム全体が動きやすくなります。

### 3. あなたの担当範囲（IoT・遠隔監視）
- **Flask + SQLite** バックエンド
- 検出受信API（`POST /upload`）、イベント取得API（`GET /events`）
- **Webダッシュボード**（遠隔監視・検出履歴・統計表示）
- 同一WiFi（LAN）内での通信

**成果物**: API、DBスキーマ、ダッシュボードUI

### 4. インターフェース契約（暫定・後で全員確定）
Aから受け取る検出イベントの JSON（暫定案）:
```json
{
  "timestamp": "2026-08-01T14:23:01",
  "type": "person",
  "confidence": 0.92,
  "image": "detections/img_0012.jpg"
}
```
- `POST /upload`: Aがこの形式で送ってくる → あなたがDBに保存
- `GET /events`: 最新の検出一覧を返す → Cやダッシュボードが読む
- このスキーマとエンドポイント名は暫定です。**あなたが叩き台を持ってきて、後で全員で確定させる役**が一番スムーズです（中心モジュールなので）。確定を待たず先に進めてOK。

### 5. 並行開発のコツ（実機・他人を待たない）
- **Aが未完成でも、あなたは単独で全部作れます。** ← これが一番の強み
- **Postman または5行のスクリプト** でダミーの検出イベントを `POST /upload` に流し込み、DB保存→ダッシュボード表示まで単体で完成させる。
  - ※Postmanがこのプロジェクトで実際に役立つのは、この **ローカルAPIデバッグ** の用途だけです。チーム共有機能などは不要。
- 実機（Pi 5）は不要。あなたのバックエンドは普通のPCで開発・テストできる。
- ハードウェアの組立・配線は **全員共通作業** です。

### 6. スケジュール（夏休みスプリント）
**開発期間 = 夏休み（7/24 〜 8月末）。締切は8月末。**
**開始はいつでもOK。実機・他モジュール・誰かを待つ必要はありません。**
- **今すぐ着手OK**: 暫定のインターフェース契約があるので、Postmanでダミーを流せばすぐ開発を始められる。Aを待つ必要はない。
- **早めに一度すり合わせ**: インターフェース契約の最終確定だけ、都合のいいタイミングで全員一度（半日）。あなたが叩き台を持参すると速い。
- **8月中〜下旬**: ハードウェア組立（全員）＋実機へ移行・3モジュール統合・テスト
- **8月末**: 完成・締切

---

## ▼ AIの準備：プロジェクト機能を使うと便利

### 毎回プロンプトを貼らずに済む方法
ClaudeとChatGPTには「**プロジェクト**」機能があり、最初に一度プロンプトを登録すれば、そのプロジェクト内のすべての会話に自動で背景が読み込まれます。毎回コピペする必要がなくなります。

**Claudeの場合**（無料プランでも可、最大5プロジェクト）:
1. 左メニューの「プロジェクト」→「＋ 新規プロジェクト」
2. 名前を付ける（例: `ARGUS バックエンド`）
3. プロジェクトの「カスタム指示（instructions）」に、下のプロンプトを貼り付け
4. （任意）この資料ファイルをプロジェクトにアップロード（ナレッジ）
→ 以降、このプロジェクト内で会話を始めれば、毎回自動で背景が効く。

**ChatGPTの場合**（無料・有料いずれも可）:
1. 左メニューの「プロジェクト（Projects）」→「新規（New）」
2. 名前を付ける
3. プロジェクト右上の「…（三点）」→「プロジェクト設定」を開き、「指示（instructions）」に下のプロンプトを貼り付け
4. （任意）プロジェクトのチャットにこの資料ファイルをドラッグしてアップロード
→ 以降、このプロジェクト内のどの会話にも自動で適用される。

---

### 貼り付け用プロンプト
```
あなたは、私が担当する「IoTバックエンド＋Webダッシュボード」の開発を手伝うアシスタントです。以下はチーム開発中のロボットプロジェクトの背景です。

【プロジェクト】ARGUS — Raspberry Pi 5 + Freenove 6脚ロボットをベースにしたAI監視ロボット。3人チーム。3層構成（歩行層=Freenove既製 / AI層=自作 / IoT層=自作）。
【私の担当】IoT層。Flask + SQLite のバックエンドと、遠隔監視用のWebダッシュボード（検出履歴・統計）。同一WiFi(LAN)内で動作。
【全体構成】スター型。私のバックエンドがシステムの中心。画像認識モジュールが検出イベント（JSON: timestamp, type, confidence, image）を「POST /upload」で送ってくる→DBに保存。音声モジュールやダッシュボードは「GET /events」で最新検出を読む。
【私のAPI（暫定）】POST /upload（検出を受信・保存）、GET /events（最新検出一覧を返す）。
【開発方針】他モジュールが未完成でも単独で開発する。Postmanや短いスクリプトでダミー検出をPOSTし、保存→ダッシュボード表示まで単体で完成させる。実機(Pi 5)は不要、普通のPCで開発する。
【技術スタック】Python3, Flask, SQLite, HTML/CSS/JS（フロントエンド）。
【期間】夏休み（7/24〜8月末）で完成。8月末締切。いつでも開始OK、誰も待たない。

この前提で、(1) Flask + SQLiteのバックエンド構成とDBスキーマ設計、(2) /upload と /events APIの実装、(3) 検出履歴と統計を表示するWebダッシュボードの作り方、について具体的なコードと手順で助けてください。まず何から着手すべきか提案してください。
```
