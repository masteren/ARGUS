# ARGUS プロジェクト — 担当C：音声対話＋歩行ブリッジ＋統合（任／リーダー）

> このドキュメントは中国語版と日本語版を併記しています。
> 本文档中文、日文双语。

---

## 🇨🇳 中文版

### 📌 关于这份文档
这是 ARGUS 项目里 **你（负责人C / 组长）负责部分** 的整理文档。
- 内容：项目介绍 → 团队分工 → 你的负责范围 → 开发方式 → 时间线 → 行走桥代码骨架。
- **最下面有给你自己 AI 的提示词，以及怎么用（把它存成 Claude/ChatGPT 的「项目」指令，就不用每次粘贴）。**

### 1. 项目介绍
**ARGUS（アーガス）** 是一台基于 6 脚机器人（Freenove Big Hexapod + Raspberry Pi 5）的 **AI 监视机器人**。用摄像头观察周围、检测人和物，信息可在远程仪表盘上监控，还能用语音和机器人对话。是 3 人团队的主力项目。

三层架构：
- **歩行层**：Freenove 官方代码（现成、不改、全员一起组装）
- **AI层（自研）**：图像识别 ＋ 语音对话
- **IoT层（自研）**：Flask 后端 ＋ Web 仪表盘

### 2. 团队分工（3人）
- **负责人A：图像识别** — Pi 5 上跑 YOLO/OpenCV，检测人/物后发给后端。
- **负责人B：后端＋仪表盘** — Flask+SQLite，接收并存储检测，Web 仪表盘远程监控。
- **负责人C：语音对话＋集成（＝你 / 组长）** — 语音和机器人对话、行走层桥接、整体集成。机器人在你手上。

模块之间松耦合。数据走 **以 B（后端）为中心的星型**：
- A（图像）→ 往 B 发检测（POST）
- **C（语音）→ 从 B 读最新检测 ＋ 往歩行层发命令**
- A 和 C 不直接通信。谁慢了都不卡住别人。

### 3. 你的负责范围（语音对话 ＋ 行走桥 ＋ 集成）
- **语音对话**：麦克风输入 → STT(语音转文字) → 云端 LLM(ChatGPT-4o / Gemini) → 文字回复 → TTS(文字转语音) → 喇叭输出
- **意图识别**：从语音里识别命令（前进/后退/转向、"你看到了什么"等）
- **行走桥**：把意图翻译成 Freenove 的命令，发给歩行层让机器人动
- **读检测**：处理"现在看到什么"这类问题时，从 B 的 `GET /events` 读最新检测，交给 LLM 回答
- **集成**：最后把三个模块 ＋ 实机合体、整体联调
- **实机**：机器人在你手上，组装/标定/电源你牵头，但全员共做

**产出**：语音对话代码、意图→命令对应表、行走桥、集成脚本

### 4. 接口契约（暂定，后面全员确定）
- **从 B 读**：`GET /events` → 返回最新检测列表（你用来回答"看到了什么"）
- **往歩行层发**：命令字符串 / 调用 Control.py（见下方「行走桥代码骨架」）
- 你不直接连 A。这套契约暂定，不用等定稿就能先开干。

### 5. 并行开发技巧（不等实机、不等别人）
- 语音链路（STT→LLM→TTS）在 **任意 PC 上** 独立做完，不需要机器人。
- 行走命令先用 **MockBridge（print）** 测，不连实机。
- "看到了什么"先用 **假数据 / 本地桩** 代替 B 的 `/events`。
- 实机就绪后，只把 `MockBridge` 换成 `FreenoveBridge`，**上层语音代码一行都不用改**。

### 6. 时间线（暑假冲刺）
**开发期 = 暑假（7/24 ～ 8月末）。截止 8月末。随时可以开始，不用等任何人。**
- **现在就能开干**：语音链路 + MockBridge，PC 上独立推进。
- **找个方便的时候全员碰一次**：把接口契约定稿（半天）。在那之前用暂定的就行。
- **8月中～下旬**：硬件组装（全员）＋ 实机迁移 ＋ 三模块集成测试 ← **这一步由你牵头**。
- **8月末**：完成、截止。

---

## 🇯🇵 日本語版

### 📌 このドキュメントについて
これは ARGUS プロジェクトで **あなた（担当C／リーダー）が担当する部分** をまとめた資料です。
- 中身: プロジェクト紹介 → チームの分担 → 担当範囲 → 開発の進め方 → スケジュール → 歩行ブリッジのコード骨格。
- **一番下に、自分用のAIプロンプトと、その使い方（Claude/ChatGPTの『プロジェクト』に登録すると毎回貼らずに済む）があります。**

### 1. プロジェクト紹介
**ARGUS（アーガス）** は、6脚ロボット（Freenove Big Hexapod + Raspberry Pi 5）をベースにした **AI監視ロボット** です。カメラで周囲を見て人や物を検出し、遠隔ダッシュボードで監視でき、音声でロボットと対話できます。3人チームの主力プロジェクトです。

3層構成:
- **歩行層**: Freenove公式コード（既製・改変しない・全員で組立）
- **AI層（自作）**: 画像認識 ＋ 音声対話
- **IoT層（自作）**: Flaskバックエンド＋Webダッシュボード

### 2. チームの分担（3人）
- **担当A：画像認識** — Pi 5上でYOLO/OpenCV。人物/物体を検出してバックエンドへ送る。
- **担当B：バックエンド＋ダッシュボード** — Flask+SQLite。検出を受信・保存し、Webダッシュボードで遠隔監視。
- **担当C：音声対話＋統合（＝あなた／リーダー）** — 音声でロボットと対話、歩行層との連携、全体統合。機体を管理。

各モジュールは疎結合。データは **B（バックエンド）を中心としたスター型** で流れます:
- A（画像）→ B へ検出を送る（POST）
- **C（音声）→ B から最新検出を読む ＋ 歩行層へコマンド**
- A と C は直接通信しない。誰かが遅れても他は止まらない。

### 3. あなたの担当範囲（音声対話 ＋ 歩行ブリッジ ＋ 統合）
- **音声対話**: マイク入力 → STT → クラウドLLM（ChatGPT-4o / Gemini）→ 応答テキスト → TTS → スピーカー出力
- **意図認識**: 音声からコマンドを判定（前進/後退/旋回、「何が見える？」など）
- **歩行ブリッジ**: 意図を Freenove のコマンドに変換し、歩行層へ送ってロボットを動かす
- **検出の参照**: 「今何が見える？」系の質問は、B の `GET /events` から最新検出を読み、LLMに渡して答える
- **統合**: 最後に3モジュール＋実機を結合し、全体を調整
- **実機**: 機体はあなたが管理。組立・標定・電源はあなたが主導、ただし全員共通作業

**成果物**: 音声対話コード、意図→コマンド対応表、歩行ブリッジ、統合スクリプト

### 4. インターフェース契約（暫定・後で全員確定）
- **Bから読む**: `GET /events` → 最新検出一覧（「何が見える？」の回答に使う）
- **歩行層へ送る**: コマンド文字列 / Control.py 呼び出し（下の「歩行ブリッジのコード骨格」参照）
- Aとは直接つながない。暫定でOK、確定を待たず着手できる。

### 5. 並行開発のコツ（実機・他人を待たない）
- 音声リンク（STT→LLM→TTS）は **任意のPCで** 単独で完成できる。実機不要。
- 歩行コマンドはまず **MockBridge（print）** でテスト。実機につながない。
- 「何が見える？」はまず **ダミーデータ / ローカルスタブ** で B の `/events` を代用。
- 実機が用意できたら `MockBridge` を `FreenoveBridge` に差し替えるだけ。**上層の音声コードは無変更。**

### 6. スケジュール（夏休みスプリント）
**開発期間 = 夏休み（7/24 〜 8月末）。締切は8月末。開始はいつでもOK、誰も待たない。**
- **今すぐ着手OK**: 音声リンク＋MockBridge をPCで独立に進める。
- **早めに一度すり合わせ**: 接口契約の最終確定だけ全員一度（半日）。それまで暫定でOK。
- **8月中〜下旬**: ハードウェア組立（全員）＋実機移行＋3モジュール統合テスト ← **あなたが主導**。
- **8月末**: 完成・締切。

---

## ▼ 行走桥代码骨架 / 歩行ブリッジのコード骨格（mock 版 + 真实 Freenove 版）

> 思路 / 考え方：上层语音模块只调用 `bridge.send(intent)`，不关心底层是 mock 还是真机。
> 开发期用 `MockBridge`，实机就绪后只换一行换成 `FreenoveBridge`，上层代码不动。
> 上位の音声モジュールは `bridge.send(intent)` を呼ぶだけ。開発期は MockBridge、実機後は1行差し替え。

```python
# robot_bridge.py
# 行走桥：把"意图"翻译成机器人命令。上层只认 send()，不管底层实现。

class RobotBridge:
    def send(self, intent: str, **kwargs):
        raise NotImplementedError


# ① 开发用：不连实机，只打印（无需机器人就能测语音链路）
class MockBridge(RobotBridge):
    def send(self, intent: str, **kwargs):
        print(f"[MOCK] robot <- {intent} {kwargs}")


# ② 实机用：连 Freenove 服务器(默认端口 5002)，发送官方命令字符串
#    —— 复用 Freenove 既有的 client 命令通道，不改动歩行层代码（白盒基础）
import socket

class FreenoveBridge(RobotBridge):
    # 意图 → Freenove 命令字符串
    # ※ 注意：下面是基于常见 Freenove 协议的【示例】。
    #   具体命令名和参数格式，请对照仓库里的 Command.py 和 Client.py 确认（版本不同可能不一样）。
    INTENT_MAP = {
        "forward":    "CMD_MOVE#1#0#35#10#0\n",
        "back":       "CMD_MOVE#1#0#-35#10#0\n",
        "turn_left":  "CMD_MOVE#1#0#35#10#10\n",
        "turn_right": "CMD_MOVE#1#0#35#10#-10\n",
        "stop":       "CMD_MOVE#1#0#0#10#0\n",
        "relax":      "CMD_RELAX\n",
        "buzzer_on":  "CMD_BUZZER#1\n",
    }

    def __init__(self, host, port=5002):
        self.sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        self.sock.connect((host, port))   # host = Pi 的 wlan0 IP（本机集成时可用 127.0.0.1）

    def send(self, intent: str, **kwargs):
        cmd = self.INTENT_MAP.get(intent)
        if cmd is None:
            print(f"[WARN] unknown intent: {intent}")
            return
        self.sock.send(cmd.encode("utf-8"))

    def close(self):
        self.sock.close()


# ───────── 用法 ─────────
# 开发期：
bridge = MockBridge()
# 实机就绪后，只换这一行（上层代码不变）：
# bridge = FreenoveBridge("192.168.x.x")

# 语音模块里，识别出意图后这样调用：
def on_voice_intent(intent):
    bridge.send(intent)        # 让机器人动
    # 然后再走 TTS 回复 "好的，前进" 之类

# 备选方案：如果集成时语音代码和歩行层跑在同一台 Pi 的同一进程里，
# 也可以不走 socket，直接 from Control import Control，调用 Control 的方法。
# 但走 TCP(端口5002)更解耦、更贴近 Freenove 官方 client 模式，推荐先用这个。
```

---

## ▼ AIの準備：プロジェクト機能を使うと便利 / AI准备：用「项目」功能更方便

### 🇨🇳 不用每次粘贴提示词的方法
Claude 和 ChatGPT 都有「**项目（Projects）**」功能：开头设置一次提示词，之后该项目里所有对话都会自动带上背景，不用每次复制粘贴。

**Claude（免费版也行，最多 5 个项目）：**
1. 左侧菜单「Projects」→「＋ New Project」
2. 取名（如 `ARGUS 语音+集成`）
3. 把下面的提示词贴进项目的「instructions（自定义指令）」
4. （可选）把这份资料文件上传到项目当 knowledge
→ 之后在这个项目里开对话，背景自动生效。

**ChatGPT（免费/付费都行）：**
1. 左侧「Projects」→「New」
2. 取名
3. 项目右上「…（三个点）」→「Project settings」→ 把提示词贴进「instructions」
4. （可选）把这份资料文件拖进项目对话里上传
→ 之后该项目里任何对话都自动套用。

### 🇯🇵 毎回プロンプトを貼らずに済む方法
ClaudeとChatGPTの「**プロジェクト**」機能で、最初に一度プロンプトを登録すれば全会話に自動適用されます。

**Claude**（無料可・最大5）: 左メニュー「プロジェクト」→「＋新規」→ 名前 → 「instructions」に下のプロンプトを貼付 →（任意）この資料をアップロード。
**ChatGPT**（無料/有料）: 左メニュー「Projects」→「New」→ 名前 → 右上「…」→「Project settings」の instructions に貼付 →（任意）資料をドラッグでアップロード。

---

### 贴り付け用プロンプト（中文版）
```
你是协助我开发「语音对话 + 行走桥接 + 系统集成」模块的助手。以下是团队开发中的机器人项目背景。

【项目】ARGUS —— 基于 Raspberry Pi 5 + Freenove 6 脚机器人的 AI 监视机器人。3 人团队。三层架构（歩行层=Freenove 现成 / AI层=自研 / IoT层=自研）。
【我的负责】语音对话 + 行走桥 + 集成。语音链路：麦克风→STT→云端 LLM(ChatGPT-4o/Gemini)→TTS→喇叭。从语音识别意图（前进/后退/转向、"看到了什么"等），把意图翻成 Freenove 命令发给歩行层让机器人动。处理"看到了什么"时从团队后端 GET /events 读最新检测，交给 LLM 回答。最后负责三模块+实机的整体集成。机器人在我手上。
【整体结构】星型。我（语音）从后端读检测、往歩行层发命令；不直接连图像模块。
【开发方针】语音链路在普通 PC 上独立开发。行走命令先用 MockBridge(print) 测，不连实机；"看到了什么"先用假数据代替 /events。实机就绪后把 MockBridge 换成连 Freenove 服务器(端口5002)的真实现，上层代码不变。
【技术栈】Python3、STT/TTS、云端 LLM API、socket(TCP)、Freenove Big Hexapod(Control.py / Command.py / 端口5002 命令)。
【周期】暑假(7/24~8月末)完成，8月末截止，随时可开始。

请基于以上，用具体代码和步骤帮我：(1) 搭语音链路(STT→LLM→TTS)、(2) 从语音稳定识别意图、(3) 行走桥(mock 版 + 真实 Freenove 版)、(4) 调 GET /events 让 LLM 回答"看到了什么"、(5) 最终三模块集成的思路。先给我从哪一步开始的建议。
```

### 貼り付け用プロンプト（日本語版）
```
あなたは、私が担当する「音声対話＋歩行ブリッジ＋システム統合」モジュールの開発を手伝うアシスタントです。以下はチーム開発中のロボットプロジェクトの背景です。

【プロジェクト】ARGUS — Raspberry Pi 5 + Freenove 6脚ロボットをベースにしたAI監視ロボット。3人チーム。3層構成（歩行層=Freenove既製 / AI層=自作 / IoT層=自作）。
【私の担当】音声対話＋歩行ブリッジ＋統合。音声リンク: マイク→STT→クラウドLLM(ChatGPT-4o/Gemini)→TTS→スピーカー。音声から意図（前進/後退/旋回、「何が見える？」等）を判定し、Freenoveのコマンドに変換して歩行層へ送りロボットを動かす。「何が見える？」のときはチームのバックエンドの GET /events から最新検出を読みLLMに答えさせる。最後に3モジュール＋実機の統合を担当。機体は私が管理。
【全体構成】スター型。私（音声）はバックエンドから検出を読み、歩行層へコマンドを送る。画像モジュールとは直接通信しない。
【開発方針】音声リンクは普通のPCで独立開発。歩行コマンドはまずMockBridge(print)でテスト、実機につながない。「何が見える？」はまずダミーデータで /events を代用。実機後はMockBridgeをFreenoveサーバ(ポート5002)に繋ぐ実装へ差し替え、上層コードは無変更。
【技術スタック】Python3, STT/TTS, クラウドLLM API, socket(TCP), Freenove Big Hexapod (Control.py / Command.py / ポート5002のコマンド)。
【期間】夏休み(7/24〜8月末)で完成、8月末締切、いつでも開始OK。

以上を前提に、具体的なコードと手順で: (1) 音声リンク(STT→LLM→TTS)の構築、(2) 音声からの安定した意図判定、(3) 歩行ブリッジ(mock版+実Freenove版)、(4) GET /events を使って「何が見える？」にLLMで答える、(5) 最終的な3モジュール統合の方針、を助けてください。まず何から着手すべきか提案してください。
```
