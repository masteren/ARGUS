# ARGUS プロジェクト — 担当B：バックエンド＋ダッシュボード＋決済（IoT／「お金が動くWebサービス」の中核）

### 📌 このドキュメントについて
これは、ARGUSプロジェクトで **あなたが担当する部分** をまとめた資料です。
- 中身: プロジェクトの紹介 → チームの分担 → あなたの担当範囲 → 開発の進め方 → スケジュール。
- **一番下に、あなた専用の「AIプロンプト」と、その使い方（AIに『プロジェクト』として登録すると毎回貼らずに済む）を用意しています。**
- ⚠️ **今回の更新点**: HEWの主題「お金が動くWebサービス」に合わせ、**「投げ銭で操作するAI監視ロボット」** という形に進化させました。観客がWebから課金してロボットを動かす——その **決済・取引履歴・公開Webページがあなたの担当に追加**され、システムの主役になります。

まず全体に目を通して、最後のプロンプトを自分のAIに設定してください。

> 💡 **この資料の細部は「叩き台」です。** エンドポイント名・JSON項目・DB列・価格・アクション名などは例にすぎません。**大きな方向性（＝お金が動くWebサービス／課金でロボット操作／履歴をDBに残す）が合っていれば、より良い設計・命名・実装を自由に変えてOK**です。AIにもそう伝えてあります。

### 1. プロジェクト紹介
**ARGUS（アーガス）** は、6脚ロボット（Freenove Big Hexapod + Raspberry Pi 5）をベースにした **AI監視ロボット** です。カメラで周囲を見て人や物を検出し、その情報を遠隔のダッシュボードで監視でき、さらに音声でロボットと対話できます。

**HEW版のコンセプト**: 観客はスマホのWebページから **ライブ映像（AI検出オーバーレイ付き）** を見て、**課金（投げ銭）してロボットにアクションやミッションを出せます**。課金の履歴はDBに保存され（HEW必須の「お金のやり取りの履歴」）、投げ銭ランキングも表示されます。お金は実際には動かさず、**Stripeテストモード**または**模擬データ**でやり取りを想定します。

3層構成:
- **歩行層**: Freenove公式コード（既製・改変しない・全員で組立）
- **AI層（自作）**: 画像認識 ＋ 音声対話
- **IoT層（自作・あなた）**: Flaskバックエンド＋決済＋Webダッシュボード ← **HEWの「お金が動くWebサービス」本体はここ**

### 2. チームの分担（3人）
- **担当A：画像認識** — Pi 5上でYOLO/OpenCV。人物/物体を検出してバックエンドへ送る。
- **担当B：バックエンド＋ダッシュボード＋決済（＝あなた）** — Flask+SQLite。検出を受信・保存、**課金を受け取り取引を記録、課金→アクションを発行**、Web（観客向け公開ページ＋運営向け監視ダッシュボード）。
- **担当C：音声対話＋統合（任／リーダー）** — 音声でロボットと対話、**課金アクション・音声意図を歩行層へ橋渡し**、全体統合。機体はCが管理。

各モジュールは疎結合です。データは **あなたのバックエンドを中心としたスター型** で流れます:
- A（画像）→ **あなた（B）へ** 検出を送る（`POST /upload`）
- **観客（Web）→ あなた（B）へ** 課金する（`POST /pay`）→ 取引を記録 ＋ アクションをキューに登録
- C（音声/統合）→ **あなた（B）から** 最新検出を読む（`GET /events`）＋ **課金アクションを取りに来る**（`GET /commands`）＋ 歩行層へコマンド
- A と C は直接通信しない。**全データ・全お金がBを経由する。**

つまり、AもCも観客も **あなたのAPIに依存** します。だからインターフェースを早く固めるほど、チーム全体が動きやすくなります。あなたが文字どおりシステムの心臓です。

### 3. あなたの担当範囲（IoT・決済・遠隔監視）
- **Flask + SQLite** バックエンド
- 検出受信API（`POST /upload`）、イベント取得API（`GET /events`）
- **【新】決済API（`POST /pay`）** — Stripeテストモード／模擬で課金を想定。**取引をDBに記録**（HEW必須）
- **【新】課金 → アクション発行** — 課金が成立したら、対応するロボット操作を **コマンドキューに登録**。Cがそれを取りに来る（`GET /commands`）
- **【新】観客向け公開Webページ** — **ライブ映像 ＋ 投げ銭/操作UI ＋ 取引履歴 ＋ 投げ銭ランキング**（← HEWで観客が触る「Webサービス」本体）
- 運営向け監視ダッシュボード（検出履歴・統計）
- 同一WiFi（LAN）内での通信

**成果物**: API、DBスキーマ（検出＋取引＋コマンドキュー）、観客向け公開Webサービス＋運営向け監視ダッシュボード

### 4. インターフェース契約（暫定・後で全員確定）
Aから受け取る検出イベントの JSON（暫定案）:
```json
{
  "timestamp": "2026-08-01T14:23:01",
  "type": "person",
  "confidence": 0.92,
  "image": "detections/img_0012.jpg"
}
```

**あなたが提供するAPI（暫定）:**
- `POST /upload` — Aが検出を送ってくる → DBに保存
- `GET /events` — 最新の検出一覧を返す → Cやダッシュボードが読む
- **【新】`POST /pay`** — 観客が課金（`{amount, action, payer_name}`）→ Stripeテスト/模擬で決済想定 → `transactions` に記録 → `command_queue` に `action` を登録 → 課金IDを返す
- **【新】`GET /commands`** — Cが未実行の課金アクションを取りに来る → `[{id, action, ...}]`
- **【新】`POST /commands/{id}/done`** — Cが実行完了を報告（任意）
- **【新】`GET /ranking`** — 投げ銭ランキング（公開ページ用）

**DBスキーマ（暫定案）:**
```
detections      (id, timestamp, type, confidence, image)
transactions    (id, timestamp, amount, action, payer_name, status)   ← HEW必須の「お金の履歴」
command_queue   (id, action, source['paid'|'voice'], status['pending'|'done'], created_at)
```

このスキーマとエンドポイント名は暫定です。**あなたが叩き台を持ってきて、後で全員で確定させる役**が一番スムーズです（中心モジュールなので）。確定を待たず先に進めてOK。

### 4.5. 具体例（すべて暫定・大方向が合えば自由に変更OK）
**課金でできること（アクションメニュー例）:**
- ¥100: 単発アクション（前進／旋回／お辞儀／威嚇モーションなど）
- ¥300: 30秒の操作権（連続でコマンドを送れる）
- ¥500: ミッション「○○を探せ」→ ロボットが巡回し、Aが対象を検出したら **成功演出＋課金者に通知**
- 投げ銭ランキング上位を「本日のオーナー」として公開ページに表示

**観客のスマホ画面（公開ページ）レイアウト例:**
- 上: ライブ映像（A検出オーバーレイ付き）
- 中: 投げ銭/操作ボタン（金額 × アクション）
- 下: 最近の取引履歴 ＋ 投げ銭ランキング

価格・演出・名称はすべて自由に変えてOK。**HEWで外せないのは「お金のやり取りがデータとして残る」ことだけ**です。検出ミッション系（¥500の例）はAと連動するので、まずは検出に依存しない単発アクション（¥100）から作ると一人で完結できます。

### 5. 並行開発のコツ（実機・他人を待たない）
- **Aが未完成でも、観客がいなくても、あなたは単独で全部作れます。** ← これが一番の強み
- **ダミー検出を `POST /upload`** に、**ダミー課金を `POST /pay`** に流し込み、保存 → 公開ページ表示 → ランキング → コマンドキューまで単体で完成させる。Postmanか5行のスクリプトでOK。
  - ※Postmanがこのプロジェクトで実際に役立つのは、この **ローカルAPIデバッグ** の用途だけです。チーム共有機能などは不要。
- **決済は薄い抽象に**: `process_payment()` のような関数で囲み、中身は最初「模擬（必ず成功を返す）」、後で「Stripeテストモード」に差し替えられるようにしておく。HEW的にはどちらでもOK（実際のお金は動かさない）。
- 実機（Pi 5）は不要。あなたのバックエンドは普通のPCで開発・テストできる。
- ハードウェアの組立・配線は **全員共通作業** です。

### 6. スケジュール（夏休みスプリント）
**開発期間 = 夏休み（7/24 〜 8月末）。締切は8月末。**
**開始はいつでもOK。実機・他モジュール・誰かを待つ必要はありません。**
- **今すぐ着手OK**: 暫定のインターフェース契約があるので、ダミーを流せばすぐ開発を始められる。Aも観客も待つ必要はない。
- **早めに一度すり合わせ**: インターフェース契約（特に `/pay` と `/commands` の形）の最終確定だけ、都合のいいタイミングで全員一度（半日）。あなたが叩き台を持参すると速い。
- **8月中〜下旬**: ハードウェア組立（全員）＋実機へ移行・3モジュール統合・テスト
- **8月末**: 完成・締切

---

## ▼ AIの準備：プロジェクト機能を使うと便利

### 毎回プロンプトを貼らずに済む方法
ClaudeとChatGPTには「**プロジェクト**」機能があり、最初に一度プロンプトを登録すれば、そのプロジェクト内のすべての会話に自動で背景が読み込まれます。毎回コピペする必要がなくなります。

**Claudeの場合**（無料プランでも可、最大5プロジェクト）:
1. 左メニューの「プロジェクト」→「＋ 新規プロジェクト」
2. 名前を付ける（例: `ARGUS バックエンド`）
3. プロジェクトの「カスタム指示（instructions）」に、下のプロンプトを貼り付け
4. （任意）この資料ファイルをプロジェクトにアップロード（ナレッジ）
→ 以降、このプロジェクト内で会話を始めれば、毎回自動で背景が効く。

**ChatGPTの場合**（無料・有料いずれも可）:
1. 左メニューの「プロジェクト（Projects）」→「新規（New）」
2. 名前を付ける
3. プロジェクト右上の「…（三点）」→「プロジェクト設定」を開き、「指示（instructions）」に下のプロンプトを貼り付け
4. （任意）プロジェクトのチャットにこの資料ファイルをドラッグしてアップロード
→ 以降、このプロジェクト内のどの会話にも自動で適用される。

---

### 貼り付け用プロンプト
```
あなたは、私が担当する「IoTバックエンド＋決済＋Webダッシュボード」の開発を手伝うアシスタントです。以下はチーム開発中のロボットプロジェクトの背景です。

【プロジェクト】ARGUS — Raspberry Pi 5 + Freenove 6脚ロボットをベースにしたAI監視ロボット。3人チーム。3層構成（歩行層=Freenove既製 / AI層=自作 / IoT層=自作）。HEWの主題「お金が動くWebサービス」に合わせ、「投げ銭で操作するAI監視ロボット」にする。観客がWebから課金してロボットにアクション/ミッションを出せる。お金は実際には動かさず、Stripeテストモードか模擬データで履歴をDBに残す。
【私の担当】IoT層（システムの中心）。Flask + SQLite のバックエンド、決済（課金受付・取引記録）、課金→ロボットアクションの発行、観客向け公開Webページ（ライブ映像・投げ銭/操作UI・取引履歴・ランキング）、運営向け監視ダッシュボード。同一WiFi(LAN)内で動作。
【全体構成】スター型。私のバックエンドが中心。画像認識モジュールが検出イベント（JSON: timestamp, type, confidence, image）を POST /upload で送る→DB保存。観客が POST /pay で課金→取引を記録しコマンドキューに登録。音声/統合モジュールが GET /events で検出を読み、GET /commands で課金アクションを取りに来る。
【私のAPI（暫定）】POST /upload（検出受信）、GET /events（検出一覧）、POST /pay（課金→取引記録＋アクション登録）、GET /commands（未実行の課金アクションを返す）、POST /commands/{id}/done（実行完了報告）、GET /ranking（投げ銭ランキング）。
【DB（暫定）】detections / transactions（お金の履歴・HEW必須）/ command_queue。
【開発方針】他モジュールや観客が未完成でも単独で開発する。ダミー検出を /upload に、ダミー課金を /pay に流し、保存→公開ページ表示→ランキング→コマンドキューまで単体で完成させる。決済は薄い抽象（process_payment関数）で囲み、最初は模擬、後でStripeテストモードに差し替え可能にする。実機(Pi 5)は不要、普通のPCで開発する。
【技術スタック】Python3, Flask, SQLite, HTML/CSS/JS（フロントエンド）, Stripeテストモード（任意）。
【期間】夏休み（7/24〜8月末）で完成。8月末締切。いつでも開始OK、誰も待たない。

この前提で、(1) Flask + SQLiteのバックエンド構成とDBスキーマ設計（検出・取引・コマンドキュー）、(2) /upload, /events, /pay, /commands APIの実装、(3) 課金→取引記録→アクション発行の流れ、(4) ライブ映像・投げ銭・操作・ランキングを表示する観客向け公開Webページの作り方、について具体的なコードと手順で助けてください。まず何から着手すべきか提案してください。

※上記のエンドポイント名・JSON項目・DB列・価格・アクション名などはすべて「叩き台」です。大きな方向性（お金が動くWebサービス／課金でロボット操作／履歴をDBに残す）が合っていれば、より良い設計・命名・実装を自由に提案・変更してかまいません。細部に縛られず、より良い案があれば積極的に提案してください。
```
