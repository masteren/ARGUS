# ARGUS — Claude Code 向けのプロジェクト案内

6脚ロボット（Freenove ＋ Raspberry Pi 5）を、観客が体験チケットで操作する展示作品。
**展示本番で観客の前で動く**ので、壊れ方が目に見える。手を入れる前にここを読むこと。

## まずこの順で読む

1. **[CONTRACT.md](CONTRACT.md)** — インターフェースの**正本**。エンドポイント・環境変数・
   アクション名・未着手タスク。**B（`ARGUS_backend/app.py`）の実装が基準**で、A と C が
   それに合わせる。契約を変えるときは、この表を先に直してから3モジュールを直す。
2. **[README.md](README.md)** — 構成・起動手順・チーム分担・ブランチ運用。
3. **[docs/DEPLOY_ROBOT.md](docs/DEPLOY_ROBOT.md)** — 実機接続、systemd、当日のトラブル対応。

## 変更したら必ず回す

```bash
bash tools/smoketest.sh          # 既定 PORT=5001。実機・カメラ・マイク・APIキー不要
```

C は書き直したコピーではなく `voice/paid_poller.py` の `poll_paid_commands` を
**そのまま import して**回すので、本物の経路を叩いている。ALL GREEN でなければ出さない。

## 触ると壊れるところ（すべて実機か review で実際に踏んだもの）

### 1. `paid_poller` の「送る直前に pending か確認する」を外すと**緊急停止が効かなくなる**
C は `GET /commands` で複数件をまとめて受け取る。その一覧はメモリにあるだけで DB と
切り離されるので、運営が停止を押して DB の pending を消しても、手元のバッチは止まらず
残りの前進を送り続ける。`_is_still_pending()` の確認がその歯止め。
**「無駄なリクエストだから」と消さないこと。**

### 2. ミッション成功にできるのは `label == "person"` のときだけ
B は `type` が `mission_` で始まる `/upload` を**無条件で**成功扱いにする。歯止めは検出側
にしか無い。`detection_lite.py` の motion（背景差分）は「動いた塊」を返すだけで、
カメラの揺れやロボット自身の歩行でも出る。これを通すと**人が居なくても ¥500 の
ミッションが完了する**。逃げ道の `ARGUS_MISSION_ACCEPT_MOTION=1` は既定 OFF のまま。
`person` を出してよいのは人だと判定できる検出器だけ（今は HOG と顔検出）。

### 3. `/control` は **localhost を常に許可**する
運営が操作するのは B を動かしている端末そのもの。ブラウザの `fetch` はトークンを
送らないので、ここをトークン必須にするとダッシュボードのボタンが**全部 403** になる。
`ARGUS_STAFF_TOKEN` は「別端末からも許す」ための追加であって、localhost の条件ではない。

### 4. 移動系は「一定時間動いて自動停止」
Freenove の `control.py` は `CMD_MOVE` をキューに残したまま繰り返す設計で、公式クライアント
は「キーを離したら停止を送る」ことで止めている。ARGUS は"チケット1枚＝1コマンド"で
"離す"が無いため、そのまま送ると**永久に歩き続ける**（実機で確認済み）。
`robot_bridge.py` が「離す」役を引き受けている。この自動停止を消さないこと。

### 5. `CMD_MOVE` の angle は **正 = 右回り / 負 = 左回り**（実機で確定）
直感と逆に見えるが、2026-09 に実機で確認して確定した。勝手に入れ替えないこと。
カメラ（頭）の上下 `CMD_HEAD#0#角度` は **角度を増やす = 上**（2026-09-23 実機で確認）。
Freenove の protocol.md は「0=水平」と書いているが誤り。実際に動く Main.py に合わせてある。

### 6. Freenove サーバーは 5002 も 8002 も **`accept()` を一度しか呼ばない**
クライアントは同時に1つだけ。公式クライアント `Main.py` を開いたままだと ARGUS が
繋がらず、逆も同じ。同時接続したいなら中継プロキシが要る（CONTRACT.md ⓪ に設計あり）。
**展示直前には入れないこと** — 動いている経路に新しい部品を挟む変更なので。

### 7. `Code/Server`（Freenove 本家）は**このリポジトリの外**
Pi 側に必要な修正は `tools/patch_freenove_server.py` が**唯一の記録**。Pi のコードを
直したら、必ずこのスクリプトにも反映する。書かなければ次に Pi を再セットアップした
瞬間に失われる。`--explain` / `--check` / `--dry-run` があり、何度当てても安全。

### 8. HTTP の宛先は `localhost` ではなく `127.0.0.1`
Windows では `localhost` が先に IPv6(`::1`) へ解決され、IPv4 で待つ B への接続が一度
失敗してから retry するので **1リクエストあたり約2秒**かかる（実測 7ms → 2050ms）。
検出ループやポーリングに効いて、目に見えて反応が遅くなる。人に見せる URL は `localhost` で良い。

### 9. カメラを開くのは **B だけ**
A は `/video_feed` を読む。1つのカメラを2プロセスで奪い合わないための設計。
実機接続時は B の中身が Freenove の映像(8002)に差し替わるだけで、**A は1行も変わらない**
（`ARGUS_backend/freenove_camera.py` が独自形式 `[4バイト長][JPEG]` を復号する）。

### 10. `ARGUS_DEBUG` は展示で必ず OFF
同一 LAN の全員に Werkzeug デバッガ＝任意コード実行が開く。

## 環境

- **macOS**: `python` は無い、`python3` のみ。AirPlay レシーバーが 5000 を掴むので `--port 5001`。
  初回起動時のファイアウォール許可を出さないと観客のスマホから繋がらない。
- **Windows（中文ロケール）**: コンソールが GBK なので、日本語を print するスクリプトは
  `PYTHONIOENCODING=utf-8` を付けないと `UnicodeEncodeError` で落ちる。
- `ultralytics`（YOLO）は PyTorch を巻き込んで 2〜3GB。**入れなくてよい** —
  `vision/detection_lite.py` が OpenCV 内蔵の検出器で枠を出す。`run_all.py --vision` が
  有無を見て自動で使い分ける。最小構成は `flask requests "opencv-python<5" numpy`。
  **OpenCV 5 は入れないこと** — HOG が本体から消えていて、A が起動直後に落ちる。

## 未解決（コードでは直せない）

- **Pi の電源不足警告**が出ている。5V/5A のアダプタが要る。放置すると**展示中に
  ランダムに再起動**しうる。ハードの問題なので人間が対応する。
- **`search_person` は実機で検証中。** 低い視点では HOG が人の全身を取れないことを確認した
  （2026-09-23）。観客は覗き込むので頭と肩しか写らない。そのため `detection_lite.py` に
  顔検出（正面＋横顔 Haar）を足し、これも `person` として扱う。カメラ角度は `ARGUS_HEAD_TILT`。
- 公開ページのミッション成功演出（CONTRACT.md ① の「公開ページ（TODO）」）。

## 言語

リポジトリのドキュメント・コードコメントは**日本語**で統一されている（チームが日本語）。
新しく書くものもそれに合わせること。
